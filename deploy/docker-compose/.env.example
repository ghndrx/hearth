# Hearth Configuration
# Copy this file to .env and configure

# Required
SECRET_KEY=change-me-to-random-32-bytes

# Public URL (for links, embeds, OAuth callbacks)
PUBLIC_URL=https://chat.example.com

# Ports
HTTP_PORT=8080
TURN_PORT=3478

# Logging
LOG_LEVEL=info

# Database
# SQLite (default):
DATABASE_URL=sqlite:///data/hearth.db
# PostgreSQL (enable 'postgres' profile):
# DATABASE_URL=postgres://hearth:password@db:5432/hearth

# PostgreSQL settings (if using postgres profile)
DB_USER=hearth
DB_PASSWORD=change-me
DB_NAME=hearth

# Redis (enable 'redis' profile for caching/multi-instance)
# REDIS_URL=redis://redis:6379

# Storage
STORAGE_BACKEND=local
# For S3 (enable 's3' profile or use external S3):
# STORAGE_BACKEND=s3
# STORAGE_ENDPOINT=http://minio:9000
# STORAGE_BUCKET=hearth
# STORAGE_ACCESS_KEY=minioadmin
# STORAGE_SECRET_KEY=minioadmin

# Registration
REGISTRATION_ENABLED=true
INVITE_ONLY=false

# MinIO console (if using s3 profile)
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
