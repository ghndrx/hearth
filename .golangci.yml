version: "2"

run:
  timeout: 5m
  exclude-files:
    - ".*_test\\.go$"

linters:
  default: none
  enable:
    - govet
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - govet

formatters:
  enable:
    - gofmt
