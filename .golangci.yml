version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - govet

formatters:
  enable:
    - gofmt

issues:
  exclude:
    - ".*_test\\.go$"
